import React, { Component } from 'react';
import { View } from 'react-native';
import { Text } from 'react-native-elements';

class EmployeeList extends Component {
  render(){
    return(
      <View>
        <Text h1>Welcome to EmployeeList</Text>
      </View>
    );
  }
};

export default EmployeeList;